# Repo-relative runtime resources that should be synced into new linked worktrees.
# `.env.local` is shared via symlink by default; other entries keep their per-resource strategy.
.env.local
.tools/.fingerprint-browser-install.json
output/registry/tavreg-hikari.sqlite
