# Repo-relative runtime resources that should be copied into new linked worktrees.
# Copy happens only when the source exists and the destination is missing.
.env.local
.tools/Chromium.app
output/registry/registry.sqlite
